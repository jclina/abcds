% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harmonization.R
\name{add_event_ids}
\alias{add_event_ids}
\title{Add Event IDs from Past Studies}
\usage{
add_event_ids(data, join_type = NULL, ...)
}
\arguments{
\item{data}{A data frame containing harmonized event data to be augmented
with study-specific event identifiers.}

\item{join_type}{The type of dplyr join to perform. Must be a string, symbol, or function (e.g.,
"inner_join", \code{inner_join}, \code{dplyr::inner_join})}

\item{...}{Variables to join by, passed as unquoted column names. These
specify the common columns between \code{data} and the event crosswalk table.}
}
\value{
A data frame with the original data augmented with study-specific
event names or IDs from the crosswalk table.
}
\description{
Joins harmonized event data with study-specific event names from
the U01 or U19 study. This function uses a crosswalk table to map between
common/harmonized event identifiers and their corresponding names
in different historical studies.
}
\details{
The function loads an internal crosswalk table (\code{event_crosswalk.RData})
from the package's \code{extdata} directory and performs a join operation using
the \code{abcds_join()} function. The crosswalk table maps harmonized event
identifiers to their equivalents across different studies.
}
\examples{
\dontrun{
# Add event IDs using a left join on 'event_name'
data_with_events <- add_event_ids(my_data, inner_join, event_sequence)
}

}
\seealso{
\code{\link{add_study_ids}} for adding study-specific participant IDs
}
