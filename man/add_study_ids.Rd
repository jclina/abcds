% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harmonization.R
\name{add_study_ids}
\alias{add_study_ids}
\title{Add Study IDs from Past Studies}
\usage{
add_study_ids(data, join_type = NULL, ...)
}
\arguments{
\item{data}{A data frame containing harmonized participant data to be
augmented with study-specific participant identifiers.}

\item{join_type}{The type of dplyr join to perform. Must be a string, symbol, or function (e.g.,
"inner_join", \code{inner_join}, \code{dplyr::inner_join})}

\item{...}{Variables to join by, passed as unquoted column names. These
specify the common columns between \code{data} and the ID crosswalk table.}
}
\value{
A data frame with the original data augmented with study-specific
participant IDs from the crosswalk table.
}
\description{
Joins harmonized participant data with study-specific participant IDs from
the U01 or U19 study. This function uses a crosswalk table to map between
common/harmonized participant identifiers and their corresponding IDs in
different historical studies.
}
\details{
The function loads an internal crosswalk table (\code{id_crosswalk.RData}) from
the package's \code{extdata} directory and performs a join operation using the
\code{abcds_join()} function. The crosswalk table maps harmonized participant
identifiers to their equivalents across different studies.
}
\examples{
\dontrun{
# Add study IDs using a left join on 'participant_id'
data_with_ids <- add_study_ids(my_data, inner_join, subject_label)

}

}
\seealso{
\code{\link{add_event_ids}} for adding study-specific event identifiers
}
